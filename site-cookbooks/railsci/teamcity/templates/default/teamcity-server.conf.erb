# teamcity-server

description	"teamcity-server"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
console output

exec su <%= node['teamcity']['user'] %> -c '/srv/TeamCity/bin/catalina.sh run'
